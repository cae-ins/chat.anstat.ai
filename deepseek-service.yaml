apiVersion: v1
kind: Service
metadata:
  name: deepseek-service
  namespace: vllm-chat
spec:
  selector:
    app: deepseek-7b
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    nodePort: 30185
  type: NodePort
